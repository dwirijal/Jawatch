# Supabase (server-side only — db.ts imports 'server-only')
SUPABASE_URL=https://<project-ref>.supabase.co
SUPABASE_ANON_KEY=<publishable/anon key>

# Site
SITE_URL=https://jawatch.vercel.app

# Auth (if/when wired)
AUTH_SHARED_COOKIE_DOMAIN=
AUTH_REDIRECT_SECRET=
